import { JobContractTypeModel } from './job-contract-type-model';

describe('JobContractTypeModel', () => {
  it('should create an instance', () => {
    expect(new JobContractTypeModel()).toBeTruthy();
  });
});
